#include <iostream>
#include <vector>

class Node
{
  Node* m_parent;

  Node* m_child;
  // Node* m_next_sibling;

public:
  static Node* union_(Node* node)
  {
     
  }
  // static Node* find_parent(Node* node)
  // {
  //   if(node->m_parent == nullptr) {
  //     return node;
  //   }

  //   return find_parent(node->m_parent);
  // }

  // void append_child(Node* child) {
  //   m_childrens.push_back(child);
  // }

};

// class DisjointSetUnion
// {
//   // std::vector<Node*> parents;
//   size_t m_size;
//   void union_(Node a, Node b);
//   size_t size()

// };

int main()
{

  return 0;
}